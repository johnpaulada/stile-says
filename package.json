{
  "name": "@johnpaulada/stile-says",
  "version": "1.0.0",
  "description": "A simon says game with characters from the Blend S anime. Made for FreeCodeCamp.",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build index.html && mkdir build && mv dist/@johnpaulada-stile-says.html build/index.html && mv dist/ build/",
    "deploy": "surge --domain https://stile-says.surge.sh --project build/"
  },
  "keywords": [
    "stile",
    "freecodecamp",
    "simon-says"
  ],
  "author": "John Paul Ada",
  "license": "MIT",
  "devDependencies": {
    "babel-plugin-transform-class-properties": "^6.24.1",
    "babel-preset-env": "^1.6.1",
    "babel-preset-stage-0": "^6.24.1",
    "parcel-bundler": "^1.6.2"
  },
  "dependencies": {
    "chance": "^1.0.13",
    "stimulus": "^1.0.1"
  }
}
